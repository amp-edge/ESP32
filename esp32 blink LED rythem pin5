#define LED_PIN 5

void setup() {
  ledcAttach(LED_PIN, 5000, 8); // pin, frequency, resolution
}

void loop() {
  for (int i = 0; i <= 255; i++) {
    ledcWrite(LED_PIN, i);
    delay(10);
  }

  for (int i = 255; i >= 0; i--) {
    ledcWrite(LED_PIN, i);
    delay(10);
  }
}
